{
  "name": "@hostguard/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.3.0"
  },
  "dependencies": {
    "@hostguard/shared": "*",
    "@hostguard/backend": "*",
    "@upstash/redis": "^1.28.0"
  }
}
